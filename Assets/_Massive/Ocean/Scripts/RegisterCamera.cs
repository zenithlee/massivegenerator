using UnityEngine;
using System.Collections;

public class RegisterCamera : MonoBehaviour {

	// Use this for initialization
	void Start () {
		//_MASSIVE.CGlobals.MainCamera = this.transform.GetComponent<Camera>();
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
